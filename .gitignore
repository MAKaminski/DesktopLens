# User config (may contain machine-specific paths / future keys)
config.sh

# Compiled native helpers (built by install.sh)
bin/lens-ocr
bin/lens-shot
bin/lens-audio

# Runtime data — raw media, text buffers, digests, state, logs, models
data/
*.wav
*.jpg
*.png
*.log
models/
ggml-*.bin

# macOS
.DS_Store
